#ifndef DEBUGGING_H
#define DEBUGGING_H

// This file and the accompanying C file will
// have several issues with it. Let's find them all!

integer fizzBuzz();

bool isTrue(int);

int makeString(){
    return 1;
}

#endif